@model List<FunctionModel>

<div class="container-xxl flex-grow-1 container-p-y">
    <h4 class="fw-bold py-3 mb-4">
        <span class="text-muted fw-light">Çöp Kutusu /</span> Silinen Fonksiyonlar
    </h4>

    <div class="card">
        <h5 class="card-header">Fonksiyon Görüntüleme Ekranı</h5>
        <div class="table-responsive text-nowrap m-5">


            <table class="table table-bordered" id="functionTables" width="100%" cellspacing="0">
                <thead>
                    <tr>
                        <th>Fonksiyon Adı</th>
                        <th>Oluşturulma Tarihi</th>
                        <th>Güncellenme Tarihi</th>
                        <th>Durum</th> @*isDeleted*@
                        <th>Eylemler</th>
                    </tr>
                </thead>
                <tfoot>
                    <tr>
                        <th>Fonksiyon Adı</th>
                        <th>Oluşturulma Tarihi</th>
                        <th>Güncellenme Tarihi</th>
                        <th>Durum</th> @*isDeleted*@
                        <th>Eylemler</th>
                    </tr>
                </tfoot>
                <tbody class="table-border-bottom-0">
                    @foreach (var item in Model)
                    {
                        <tr name="@item.Id">
                            <td>@item.FunctionName</td>
                            <td>@item.CreatedDate.ToShortDateString()</td>
                            <td>@item.UpdatedDate</td>
                            <td>@(item.IsDeleted ? "Aktif Değil" : "Aktif")</td>
                            <td>
                                <a class="btn btn-warning btn-sm" data-id="@item.Id" asp-controller="Function" asp-action="UndoDelete" asp-route-id="@item.Id"><i class="bx bx-undo"></i></a>
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>
    </div>

</div>

@section Scripts{
    <script src="~/js/functionIndex.js" type="application/ecmascript"></script>
}